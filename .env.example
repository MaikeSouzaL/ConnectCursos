# Conect Cursos — variáveis de ambiente (frontend)
# Copie para .env e preencha com os valores do seu projeto Supabase.
# A anon/publishable key é pública (pode ir no bundle do front); NUNCA use a service_role aqui.

VITE_SUPABASE_URL=https://uimuifgbeubrvvpbzvrl.supabase.co
VITE_SUPABASE_ANON_KEY=
